import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/byok-academy/',
})
// Re-applying base configuration to trigger commit for the user's issue
